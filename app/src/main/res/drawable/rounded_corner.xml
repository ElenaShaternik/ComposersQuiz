<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <stroke
        android:width="@dimen/rounded_corner"
        android:color="@color/toast"
        android:background="@color/toast"
        />

    <solid android:color="@color/toast" />

    <padding
        android:left="@dimen/rounded_corner"
        android:right="@dimen/rounded_corner"
        android:top="@dimen/rounded_corner" />

    <corners android:radius="@dimen/rounded_corners_radius" />
</shape>